# Write Kaggle Benchmarks Skill

This skill teaches AI coding agents how to write, push, run, and manage Kaggle Benchmark tasks using the `kaggle` CLI and the `kaggle-benchmarks` Python SDK.

## Structure

```
skills/write-kaggle-benchmarks/
├── SKILL.md    # Complete skill file — quick reference, API docs, and core workflow
└── README.md   # This file
```

## Installation

Point your AI coding agent to read `SKILL.md`.

**Gemini CLI** — Copy to the auto-discovery location:
```bash
cp -r skills/write-kaggle-benchmarks ~/.gemini/skills/write-kaggle-benchmarks
```

**Other tools** — Ask your agent to read `skills/write-kaggle-benchmarks/SKILL.md`.
